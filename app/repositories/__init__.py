"""数据访问层(Repository)。

这一层的唯一职责:把"怎么读写数据库"封装起来。

规矩(很重要,分层能不能守住全靠它):
- 只依赖 sqlmodel / models,绝不 import fastapi
- 不做业务判断(比如"密码对不对"),只负责取数据和存数据
- 不抛 HTTPException,数据库层面的异常原样往上冒

好处:上层业务代码里看不到一句 SQL。以后表结构调整、
加软删除过滤、换 ORM,改动都被关在这一层里面。
"""
