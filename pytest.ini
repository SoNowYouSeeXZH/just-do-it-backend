[pytest]
testpaths = tests
# 门槛设为「实测值 −2」而不是一个宽松的整数:实测 89.15%,门槛 87。
# 留 2 个点是给正常波动,不是给退化。门槛远低于实测等于没有门槛——
# 覆盖率从 89% 掉到 81% 时 CI 仍然全绿,这种门禁只提供虚假安全感。
# omit 清单见 .coveragerc,每项都写了理由与退场条件。
addopts = --cov=app --cov-report=term-missing --cov-fail-under=87
