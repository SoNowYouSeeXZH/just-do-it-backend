# 开发/测试依赖。生产镜像只装 requirements.txt,不装本文件,
# 避免把测试框架带进线上环境(镜像更小、攻击面更少)。
#
# 安装:.venv/bin/python -m pip install -r requirements-dev.txt

# pytest:Python 事实标准的测试框架
pytest==8.3.4
# httpx:FastAPI 的 TestClient 底层依赖它发请求(不用真的起服务器监听端口)
httpx==0.28.1
# pytest-cov:统计测试覆盖了哪些代码分支
pytest-cov==6.0.0
# fakeredis:纯 Python 实现的 Redis 替身,行为和真 Redis 一致(含 TTL、SCAN)。
# 用它而不是 mock 掉整个缓存模块:mock 只能验证"调用了 set",
# 无法验证"写进去的值能被正确读回来",而后者才是缓存逻辑的关键。
fakeredis==2.26.2

# ===== 迁移窗口期临时依赖(数据迁完整体删除)=====
# 下面两个只被 app/migrate_mysql_to_pg.py 用到。该脚本已于 2026-09 实际执行完毕,
# 仅作留档;确认不再需要复跑后,连同这一整段一起删除。
# PyMySQL:读旧 MySQL 源库用。生产镜像只装 requirements.txt,不包含它。
PyMySQL==1.1.1
# python-dotenv:迁移脚本直接读 .env 里的 MYSQL_* 变量(Settings 已不再声明它们)。
# 此前靠 pydantic-settings 的传递依赖侥幸可用——隐式依赖在干净环境会 ImportError,
# 用到就必须显式声明。
python-dotenv==1.2.2
